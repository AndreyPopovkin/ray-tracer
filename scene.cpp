#include "scene.h"

void Scene::pushSphere(const Sphere&);

Scene::Scene(const Point& eye_, const Tetragon& screen_, int widthScreenResolution_, int widthScreenResolution_) {

}

void Scene::update();

void Scene::pushStar(const Point&);
